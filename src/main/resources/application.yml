spring:
  ai:
    mcp:
      server:
        name: mcp-stack-de-mama
        version: 0.0.1
        protocol: STREAMABLE
server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: mappings

logging:
  level:
    org.springframework.web: DEBUG